import type { PerformanceFinding, PerformanceRule } from "../model/types";
import { callName, finding, visit } from "./ast-utils";
import { isConfiguredCritical } from "./critical-path-utils";
const definitions = [["performance.transaction.external-call-in-critical-section", "External call in critical transaction path", "A configured critical path issues an external request.", "Move nonessential work out of the critical section or use a bounded dependency boundary."], ["performance.transaction.unbounded-fanout", "Unbounded fan-out in critical transaction path", "A configured critical path fans out dynamic concurrent work.", "Apply a bounded concurrency policy before entering the transaction path."]] as const;
export const transactionPerformanceRules: readonly PerformanceRule[] = definitions.map(([id, title, message, suggestion]) => ({ meta: { id, title, description: message, category: "transaction", defaultSeverity: "high", defaultConfidence: "high" }, check(context) { const findings: PerformanceFinding[] = []; visit(context.ast, (node, ancestors) => { if (!isConfiguredCritical(ancestors, context.criticalEntrypoints)) return; if (id === "performance.transaction.external-call-in-critical-section" && node.type === "CallExpression" && ["fetch", "request"].includes(callName(node) ?? "")) findings.push(finding(this, context, node, message, suggestion)); if (id === "performance.transaction.unbounded-fanout" && node.type === "CallExpression" && callName(node) === "all" && node.arguments[0]?.type === "CallExpression" && callName(node.arguments[0]) === "map") findings.push(finding(this, context, node, message, suggestion)); }); return findings; } }));
