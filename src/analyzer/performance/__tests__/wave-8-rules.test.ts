import { describe, expect, it } from "vitest";
import { parseSource } from "../../ast/parser";
import { bankUiPerformanceRules, observabilityPerformanceRules, PerformanceAnalysisEngine, PerformanceRuleRegistry, transactionPerformanceRules, type PerformanceRule } from "..";
function ids(source: string, rules: readonly PerformanceRule[], options: { readonly criticalEntrypoints?: readonly string[]; readonly criticalUiComponents?: readonly string[]; readonly telemetryCallPaths?: readonly string[] } = {}): readonly string[] { const registry = new PerformanceRuleRegistry(); rules.forEach((rule) => registry.register(rule)); return new PerformanceAnalysisEngine().analyze({ source, file: "payment.tsx", ast: parseSource(source), ...options }, registry).map((finding) => finding.ruleId); }
describe("wave 8 performance rules", () => { it("requires configured banking entrypoints", () => { const source = "function submitPayment(){ return fetch('/pay'); }"; expect(ids(source, transactionPerformanceRules, { criticalEntrypoints: ["submitPayment"] })).toContain("performance.transaction.external-call-in-critical-section"); expect(ids(source, transactionPerformanceRules)).toEqual([]); }); it("requires a configured telemetry adapter before observability checks", () => { const source = "function submitPayment(){ return fetch('/pay'); }"; expect(ids(source, observabilityPerformanceRules, { criticalEntrypoints: ["submitPayment"], telemetryCallPaths: ["telemetry.span"] })).toContain("performance.observability.external-call-without-timing-context"); expect(ids(source, observabilityPerformanceRules, { criticalEntrypoints: ["submitPayment"] })).toEqual([]); }); it("checks configured critical UI components only", () => { const source = "function PaymentForm(){ return items.sort(); }"; expect(ids(source, bankUiPerformanceRules, { criticalUiComponents: ["PaymentForm"] })).toEqual(["performance.bank-ui.blocking-critical-render"]); }); });
