import { describe, expect, it } from "vitest";
import { parseSource } from "../../ast/parser";
import { analyzeInterproceduralPerformance } from "..";
describe("interprocedural performance analysis", () => { it("propagates known cost kinds through local wrappers", () => { const source = "function load(){ return fetch('/api'); } function page(){ return load(); }"; const result = analyzeInterproceduralPerformance({ source, file: "service.ts", ast: parseSource(source) }); expect(result.summaries.find((summary) => summary.name === "page")?.costKinds).toEqual(["external-service", "network"]); }); it("terminates recursive cycles and preserves unknown calls", () => { const source = "function a(){ return b(); } function b(){ return a(); } function c(){ return vendor(); }"; const result = analyzeInterproceduralPerformance({ source, file: "service.ts", ast: parseSource(source) }); expect(result.summaries.find((summary) => summary.name === "c")?.unknownCalls).toEqual(["vendor"]); expect(result.summaries.find((summary) => summary.name === "a")?.costKinds).toEqual([]); }); });
