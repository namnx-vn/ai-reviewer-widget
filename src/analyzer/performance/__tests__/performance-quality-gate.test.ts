import { describe, expect, it } from "vitest";
import { evaluatePerformanceQualityGate, type PerformanceFinding } from "..";
const finding: PerformanceFinding = { id: "new-payment", ruleId: "performance.transaction.external-call-in-critical-section", title: "title", message: "message", severity: "high", confidence: "high", category: "transaction", location: { path: "payment.ts" }, evidence: [{ message: "evidence" }] };
describe("performance quality gate", () => { it("fails new banking-critical findings after profile policy", () => expect(evaluatePerformanceQualityGate({ findings: [finding], profile: "performance/banking", evaluatedAt: "2026-08-05T10:00:00Z" }).decision).toBe("fail")); it("baselines legacy debt and honors auditable suppressions", () => { expect(evaluatePerformanceQualityGate({ findings: [finding], profile: "performance/banking", evaluatedAt: "2026-08-05T10:00:00Z", baselineFindingIds: [finding.id] }).decision).toBe("pass"); expect(evaluatePerformanceQualityGate({ findings: [finding], profile: "performance/banking", evaluatedAt: "2026-08-05T10:00:00Z", suppressions: [{ ruleId: finding.ruleId, reason: "tracked migration", expiresAt: "2026-09-01T00:00:00Z" }] }).decision).toBe("pass"); }); });
