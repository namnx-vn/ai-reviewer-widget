# Phase 3.7.12 — Resilience, Timeout & Retry

> Parent: [Phase 3.7](./README.md)

Status: ✅ Complete

## Objective

Detect call patterns that can hang, amplify incidents, or exceed latency budgets under partial failure.

## Rules

```text
performance.resilience.missing-timeout
performance.resilience.unbounded-retry
performance.resilience.retry-without-backoff
performance.resilience.retry-without-jitter
performance.resilience.retry-non-idempotent
performance.resilience.retry-amplification
```

## Detection Contract

Timeout detection requires modeled client/config semantics. Unknown libraries MUST NOT be treated as definitely missing timeouts.

Retry semantics MUST distinguish bounded retries, exponential/backoff helpers, idempotent operations, and transaction-sensitive operations.

## Implementation

Implemented in `src/analyzer/performance/rules/resilience-rules.ts`.

The current deterministic model intentionally limits definite timeout findings to modeled `fetch` semantics. It recognizes visible abort/timeout evidence, bounded versus obviously unbounded retry loops, backoff/delay calls, jitter evidence, non-idempotent fetch methods, and nested retry amplification.

Unknown client libraries are not classified as definitely missing timeout configuration.

## Tests

Covered by `src/analyzer/performance/__tests__/wave-5-rules.test.ts`:

- fetch without timeout;
- AbortSignal timeout evidence;
- unbounded retry loops;
- retry without backoff;
- backoff with jitter;
- non-idempotent retry behavior.

## Validation

Validated through CI after implementation:

```text
TypeScript  ✅
Lint        ✅
Tests       ✅ 74 files / 602 tests
Build       ✅
```
